import 'package:flutter/material.dart';

class ScheduleTime extends StatelessWidget {
  const ScheduleTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ScheduleTime AND payment'),
      ),
      body: const Column(
        children: [],
      ),
    );
  }
}
